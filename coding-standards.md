# Coding Standards

- Use Python 3.11+.
- Keep functions small and focused.
- Add type hints to public functions.
- Use clear docstrings for public functions.
- Prefer the standard library unless a dependency is necessary.
- Every behavior change must have automated tests.
- Run `pytest` before creating a pull request.
- Do not modify unrelated files.
